# ── Core ──────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.3
python-dotenv==1.0.1
colorama==0.4.6

# ── Backend ───────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ── Database ──────────────────────────────────────
# sqlite3 is part of Python stdlib — no install needed

# ── AI Layer (single OpenAI-compatible provider) ──
openai>=1.30.0          # OpenAI GPT-4o, and all OpenAI-compatible endpoints

# ── Terminal UI (terminaltui, Node — see terminal-tui/) ──
# The TUI is a Node app (`npx webvapt-tui`) that calls the FastAPI
# scan engine over HTTP. No Python TUI dependencies remain.

# ── Dev / Testing ─────────────────────────────────
pytest>=8.4,<9
pytest-asyncio>=1.4.0
ruff>=0.4.0             # linting
bandit>=1.7.9           # SAST on this codebase itself
