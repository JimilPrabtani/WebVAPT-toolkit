# ── Core ──────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.3
python-dotenv==1.0.1
colorama==0.4.6

# ── Backend ───────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ── Database ──────────────────────────────────────
# sqlite3 is part of Python stdlib — no install needed

# ── AI Layer (single OpenAI-compatible provider) ──
openai>=1.30.0          # OpenAI GPT-4o, and all OpenAI-compatible endpoints

# ── TUI (black-and-white terminal: python tui.py) ───
textual>=0.86

# ── Web terminal (uvicorn main:app → open /terminal) ──
pywinpty>=2.0.0; sys_platform=='win32'   # ConPTY backend that runs tui.py per browser session

# ── Dev / Testing ─────────────────────────────────
pytest>=8.4,<9
pytest-asyncio>=1.4.0   # headless Textual Pilot tests (tests/test_tui.py)
ruff>=0.4.0             # linting
bandit>=1.7.9           # SAST on this codebase itself
